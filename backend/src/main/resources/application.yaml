spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sany3_db
    username: root
    password: Aa0552635037#
    driver-class-name: com.mysql.cj.jdbc.Driver

  flyway:
    enabled: true
    baselineOnMigrate: true
    baselineVersion: 0
    locations: classpath:db/migration
    validateOnMigrate: false
    outOfOrder: false

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

jwt:
  secret: mySecretKeyForSanay3ApplicationThatIsLongEnoughForHS256
  access-token-expiry: 900000
  refresh-token-expiry: 604800000

logging:
  level:
    root: INFO
    com.sanay3: DEBUG
    org.springframework.security: DEBUG